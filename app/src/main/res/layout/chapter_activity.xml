<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <TextView
        android:id="@+id/tvDeckTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:text="Nombre del deck"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnEditDeck"/>

    <ImageButton
        android:id="@+id/btnEditDeck"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/card_bg"
        android:src="@drawable/ic_edit_deck"
        android:contentDescription="Editar deck"
        app:layout_constraintTop_toTopOf="@id/tvDeckTitle"
        app:layout_constraintBottom_toBottomOf="@id/tvDeckTitle"
        app:layout_constraintEnd_toEndOf="parent"
        android:backgroundTint="@color/secondaryColor"
        />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnCreateChapter"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Crear capítulo"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvDeckTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/card_bg"
        android:backgroundTint="@color/secondaryColor"
        />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnStudyDeck"
        android:text="Estudio completo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/btnCreateChapter"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/card_bg"
        android:backgroundTint="@color/primaryColor"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerChapters"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/btnStudyDeck"
        app:layout_constraintBottom_toTopOf="@id/btnQuickStudy"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="12dp"
        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnQuickStudy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="Quick Study"
        android:src="@drawable/ic_temp_quick_study"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:shapeAppearanceOverlay="@style/CustomFabShape"
        />

</androidx.constraintlayout.widget.ConstraintLayout>